[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "AdaptEns"
version = "0.1.0"
description = "Adaptive Selection of ECMWF Ensemble using FSS displacement ranking"
authors = [
    {name = "Rovie S. de Ramos"}
]
requires-python = ">=3.11"
